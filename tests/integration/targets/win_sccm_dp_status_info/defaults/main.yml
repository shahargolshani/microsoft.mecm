---
# Default test variables for dp_status_info module testing

# SCCM Server Configuration - will be set dynamically in tasks
sccm_server_name: ""

# SCCM Site Code - set to default for tests
test_site_code: "ECO"

# Distribution Point Configuration - will be set dynamically in tasks
test_distribution_point: ""

# Distribution Point Group - override per environment if needed
test_dp_group: "primary"

# Package Configuration - using actually distributed package
test_package_id: ECO00003

# Test environment flags
has_sccm_config: true
